id = "instanceofExpectObjects";
title = "instanceof Expect Objects";
description = "PHP used to report a fatal error when provided with a value that is not an object. After PHP 7.3, it would return false in such case, and not break the execution.";
code = "<?php

var_dump(null instanceof Countable);

function foo() : ?X { /**/ }
var_dump(foo() instanceof Countable); // possible error when foo() returns null

?>";
before = "PHP Fatal error:  instanceof expects an object instance, constant given 

Fatal error: instanceof expects an object instance, constant given 
";
after = "bool(false)
";
seeAlso[Type Operator] = "https://www.php.net/manual/en/language.operators.type.php#language.operators.type";
deprecation = "";
phpVersion = "7.3";
alternatives[] = "Check the type of the tested object before using ``instanceof``.";
alternatives[] = "Upgrade to PHP version 7.3 or more recent.";
related[] = "";
phpError["instanceof expects an object instance, constant given"] = "instanceof-expects-an-object-instance,-constant-given";
keywords[] = "removed later";
features[] = "instanceof";
features[] = "scalar-type";
features[] = "object";
