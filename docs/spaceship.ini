id = "spaceship";
title = "spaceship Operator Results";
description = "With the change of comparison between integers and strings, the spaceship was also impacted. Some spaceship comparisons did change, and are not returning the same results than before. ";
code = "<?php

var_dump( 0 <=> 'foo');
var_dump( 0 <=> '');

?>";
before = "int(0)
int(0)";
after = "int(-1)
int(1)";
seeAlso[] = "";
deprecation = "";
phpVersion = "8.0";
alternatives[] = "Cast both operands to the same type.";
related[] = "";
phpError[] = "none";
keywords[] = "integer string comparison";
keywords[] = "silent";
